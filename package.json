{
  "name": "web-project",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build --if-present && cd ../backend && npm install && npm run build --if-present",
    "start:prod": "node backend/dist/index.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
